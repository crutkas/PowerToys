[package]
name = "cursorwrap-core"
version = "0.1.0"
edition = "2024"
description = "CursorWrap core logic — monitor topology, edge detection, and cursor wrapping (no Win32)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
