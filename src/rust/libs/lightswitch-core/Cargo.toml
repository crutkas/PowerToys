[package]
name = "lightswitch-core"
version = "0.1.0"
edition = "2024"
description = "LightSwitch core logic — schedule, theme detection, settings (no Win32)"


[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
