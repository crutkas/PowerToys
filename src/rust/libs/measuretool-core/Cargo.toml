[package]
name = "measuretool-core"
version = "0.1.0"
edition = "2024"
description = "MeasureTool core logic — edge detection, measurement calculation, and settings (no Win32)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
